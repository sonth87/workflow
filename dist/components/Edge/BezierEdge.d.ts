import { Edge, EdgeProps } from "@xyflow/react";
declare const BezierEdgeConnectionLine: ({ id, sourceX, sourceY, targetX, targetY, sourcePosition, targetPosition, style, markerEnd, data, }: EdgeProps<Edge<{
    label: string;
}>>) => import("react/jsx-runtime").JSX.Element;
export default BezierEdgeConnectionLine;
